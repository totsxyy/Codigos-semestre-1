# informações de sistema
$ cat renata.sh
#!/user/bin/bash
echo "======== informações do sistema =========="
#if [ $(uname -a) = 'uname -a' ];
        echo "$(uname -a)"
 
 
echo "===informações do usuario==="
#if [ $(whomi) = 'root' ];
        echo "Nome do usuario: $(whoami)"
#elif [ $(cd /) = '/' ];
        echo "diretorio home: $(cd /)"
#elif [ $(pwd) = 'pwd' ];
echo "diretorio atual: $(pwd)"
 
#elif [ $(w -q) = 'w -q' ];
        echo "usuarios logados: $(w -q)";
#fi
